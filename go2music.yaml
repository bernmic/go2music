application:
  mode: debug
  loglevel: debug
  cors: all
server:
  port: 8080
database:
  username: go2music
  password: go2music
  schema: go2music
  type: mysql
  url: ${username}:${password}@tcp(localhost:3306)/${schema}
databasep:
  username: root
  password:
  schema: go2music?sslmode=disable
  type: postgres
  url: postgresql://${username}:${password}@localhost:26257/${schema}
media:
  path: m:/
  syncfrequency: 300s
  syncatstart: true
